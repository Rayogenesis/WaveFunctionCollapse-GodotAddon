name: Build and Package WFC Plugin

on:
  push:
    tags:
      - "v*"

jobs:
  build-desktop:
    strategy:
      matrix:
        platform: [windows, linux, macos]
      fail-fast: false  # ¡Nuevo! Evita que fallen otros jobs
    runs-on: ${{ matrix.platform }}-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Python
        uses: actions/setup-python@v5

      - name: Install SCons
        run: python -m pip install scons

      - name: Build for ${{ matrix.platform }}
        shell: bash
        run: |
          cd GDExtension
          scons platform=${{ matrix.platform }} target=template_release -j4
          mkdir -p ../addons/wfc/bin/${{ matrix.platform }}
          cp libwfc.* ../addons/wfc/bin/${{ matrix.platform }}/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}
          path: addons/wfc/bin/${{ matrix.platform }}/*

  build-android:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [arm64-v8a, x86_64]
      fail-fast: false
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Android NDK
        uses: actions/setup-android@v2
        with:
          ndk-version: 25c
          packages: ndk;25.2.9519653

      - name: Install dependencies
        run: |
          python -m pip install scons

      - name: Build for Android (${{ matrix.arch }})
        run: |
          cd GDExtension
          scons platform=android target=template_release android_arch=${{ matrix.arch }} -j4
          mkdir -p ../addons/wfc/bin/android
          cp libwfc.* ../addons/wfc/bin/android/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-${{ matrix.arch }}
          path: addons/wfc/bin/android/*

  package-release:
    needs: [build-desktop, build-android]
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: '*'
          merge-multiple: true

      - name: Create package
        run: |
          mkdir -p package/addons/wfc
          cp -r addons/wfc/* package/addons/wfc/
          cp LICENSE package/addons/wfc/
          
          # Organize binaries
          mkdir -p package/addons/wfc/bin/{windows,linux,macos,android}
          cp artifacts/windows/* package/addons/wfc/bin/windows/
          cp artifacts/linux/* package/addons/wfc/bin/linux/
          cp artifacts/macos/* package/addons/wfc/bin/macos/
          cp artifacts/android-*/* package/addons/wfc/bin/android/

      - name: Create ZIP
        run: |
          cd package
          zip -r ../wfc-plugin-${{ github.ref_name }}.zip *

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: wfc-plugin-${{ github.ref_name }}.zip
          tag_name: ${{ github.ref_name }}